# 2026-03-19

## plan.md 压缩重写
- 将 `plan/active/desktop-ui-shell-refactor/plan.md` 从 626 行压缩到约 220 行
- 删除了大量 Electron 历史叙事、已完成阶段的冗长描述、重复的非目标/风险段落
- 补充了新架构真实现状：tauri/ 工程结构、依赖清单、页面/组件清单、Phase 6 验证状态
- Phase 0-6 统一用表格简要标注完成状态，Phase 7（E2E 自动化测试）标为待执行
- 已知功能缺口独立成表

## plan.md 阶段路线调整
- 插入 Phase 7（UI 交互完整性补全）：功能缺口表收为正式阶段
- 插入 Phase 8（后端测试工程迁移）：Jvedio.Test 深度绑定旧 WPF，需迁移到 .NET 8 + Worker/Contracts
- 原 E2E 测试顺延为 Phase 9 并标注暂缓（公共电脑环境，数据模拟敏感）
- 同步更新 doc/testing/README.md：后端测试加迁移待办说明，前端 E2E 加暂缓标注

## plan.md 阶段路线再调整
- 插入 Phase 9（日志目录统一）：将散落各处的日志收敛到 `{repo}/log/` 下 runtime/test/dev 分层
- Phase 9 明确标注：启动时先重新收集 Phase 8 后的实际日志输出情况再执行
- 原 E2E 测试顺延为 Phase 10
- Phase 8 第6步改为直接物理删除旧测试工程（非 deprecated）

## Phase 7-10 三轮迭代完善
基于代码核实和关联文档全面审查，对 Phase 7-10 做了深度迭代：
- **Phase 7**：剔除已完成项（"打开文件夹"已接入 opener API、右键菜单已实现），Settings 细化到 3 个子组具体需实现的表单，新增批量操作项和前置依赖检查，补充 5 份关联文档更新清单
- **Phase 8**：从 6 步扩展为 8 步（新增 sln 修改、PS1 脚本重写），迁移表细化到每个测试的类型和注意事项，补充 9 份关联文档更新清单
- **Phase 9**：新增启动时收集清单（7 项），代码改动表细化到具体文件，补充 7 份关联文档更新清单
- **Phase 10**：拆为环境准备 / 数据播种 / 用例执行三个子步骤，加入 48 用例分 Flow 统计表，补充 8 份关联文档更新清单

## Phase 8 后端测试工程迁移（已完成）
- 新建 `Jvedio.Worker.Tests`（.NET 8 / MSTest 3.x / WebApplicationFactory<Program>）
- 13 个契约测试全部通过：Bootstrap(2) + DTO(2) + Libraries(2) + Settings(3) + Videos(4)
- 修复 3 个测试断言错误：camelCase 序列化策略、CreateLibrary 嵌套响应结构、UpdateSettings 必填字段
- 旧 Jvedio.Test 从 sln 移除，评估结论：0 个可直接迁移，5 个高价值需重写（标记技术债）
- 旧 Jvedio.Test 目录用户拒绝物理删除，暂保留
- 关联文档更新完成：AGENTS.md、testing/README.md、test-plan.md、test-targets.md、test-current-suite.md、developer.md、CHANGELOG.md、handoff.md

## Phase 8 收尾：旧测试删除 + 业务测试重写（已完成）
- 物理删除旧测试工程 `Jvedio-WPF/Jvedio.Test/` 目录
- 重写 5 个高价值旧测试到 `BusinessLogicTests/` 目录：
  - VidParsingTests: 17 个数据驱动用例（通过反射调用 ExtractVideoId）
  - SidecarPathTests: 6 个用例（验证 NFO/poster/thumb/fanart 路径，读写一致性）
  - ScanOrganizeTests: 5 个文件系统用例（通过反射调用 TryOrganize）
  - ScanImportApiTests: 2 个 API 端到端用例（扫描导入 + 空目录）
- 测试总数从 13 → 44，全部通过
- 关联文档全部更新
- 已提交推送到 origin/master (d165658)

## 旧 Jvedio.Test 目录彻底清理（已完成）
- 上次物理删除未完全生效（文件仍在磁盘且 41 个文件仍被 git 跟踪）
- 在 `temp/remove-old-test.ps1` 编写清理脚本：Remove-Item + git rm --cached
- 脚本执行后 41 个文件从 git index 移除，目录从磁盘彻底删除
- 已提交推送到 origin/master (2314dc7)

## Phase 8.5 目录更名 `Jvedio-WPF/` → `dotnet/`（已完成）
- `git mv Jvedio-WPF dotnet` 物理更名
- 修改运行时代码 4 个文件：Program.cs（repo root 检测 + 注释）、worker.rs（dev 路径）、shell_log.rs（repo 检测）、prepare-worker.ps1（构建路径）
- 批量替换 29 个文档文件、171 处引用，全局搜索确认零残留
- Worker 编译通过、44 个测试全部通过
- 已提交推送到 origin/master (40e5f61)，521 文件变更

## Phase 9 日志目录统一（已完成）
- `log/` 从平铺结构改为 `runtime/` + `test/` + `dev/` 分层子目录
- Worker `ResolveLogDirectory()` → `log/runtime/`，`JVEDIO_LOG_DIR` 自动追加 `runtime/`
- Shell `resolve_log_dir()` → `log/runtime/`，同样自动追加
- TestBootstrap `JVEDIO_LOG_DIR` 指向 `log/test/worker-tests/`
- 创建 5 个 `.gitkeep` 文件保留目录结构
- `.gitignore` 更新支持子目录例外
- `doc/logging-convention.md` 重写为分层结构
- 关联文档更新：AGENTS.md、testing/README.md、test-plan.md、handoff.md、CHANGELOG.md、plan.md
- Worker 编译通过、44 个测试全部通过
- 已提交推送到 origin/master (6538d72)

## Plan.md 已完成内容简化 + AGENTS 规则
- 简化 plan.md 中 Phase 6-9 的已完成部分：从详细步骤/表格/通过标准压缩为 2-5 行摘要
- plan.md 从 ~645 行压缩到 ~330 行
- 在 AGENTS.md 增强规划工作流中新增规则：完成步骤后必须回 plan.md 简化已完成内容

## 新建模块文档 13-tauri-shell-worker.md
- 创建 `doc/modules/13-tauri-shell-worker.md`：Tauri Shell 与 Worker 连接机制文档
- 内容涵盖：架构总览、完整启动流程（8 步）、动态端口发现协议、四种运行模式对比、关键源文件索引、Tauri 事件清单、故障排查表
- 同步更新 `doc/developer.md` 模块索引表，添加第 13 条记录

## Phase 10 E2E 测试方案完善
- 大幅扩展 plan.md 中 Phase 10 内容，从 60 行扩展到约 170 行
- 新增 10.2 测试数据目录结构：完整的临时目录树 + 与 Release 版对比表（10 个维度）
- 新增 10.3 假视频文件要求：VID 命名规则、文件内容与大小要求（≥1KB）、5 个播种文件清单
- 新增 10.4 扫描后数据位置：整理前后目录变化对比、SQLite 记录结构、Sidecar 文件位置
- 新增 10.5 播种脚本详细步骤（10 步，含 API 调用和验证点）
- 新增 10.7 测试后清理流程
- 核心设计：通过 `JVEDIO_APP_BASE_DIR` 环境变量将数据重定向到临时隔离目录，与后端 TestBootstrap 使用相同策略

## Phase 10 文档拆分：plan → doc 持久文档
- 将 plan.md Phase 10 中约 120 行的规范性内容（数据目录、假文件要求、扫描后数据位置、与 Release 版对比）抽离到 doc/ 目录
- 新建 `doc/data-directory-convention.md`：Release 版完整目录树、SQLite 表结构、缓存路径、路径推断规则（3 级优先级）、环境变量汇总、三种运行模式对比表
- 新建 `doc/testing/e2e/e2e-test-data-spec.md`：E2E 测试临时目录结构、假视频文件要求（VID 命名、内容、大小）、5 个播种文件清单、扫描后目录变化、播种脚本 10 步流程、清理流程
- plan.md Phase 10 精简为 4 个子步骤（10.1 环境准备、10.2 测试数据与环境、10.3 用例执行、10.4 测试后清理），详细内容全部改为文档引用
- 关联文档索引更新：doc/testing/README.md、playwright-e2e-test-plan.md、doc/developer.md

## E2E 测试目录方案调整：timestamp → 固定名称
- 将 E2E 测试数据根目录从 `jvedio-e2e-{timestamp}` 改为固定名称 `jvedio-e2e/`
- 播种脚本改为"先清空再重建"策略，取代"每次新建带时间戳的目录"
- 理由：日志路径稳定、不累积垃圾、真正幂等、调试友好；E2E 是单进程串行不需要并行隔离
- 涉及文件：e2e-test-data-spec.md（目录结构 + 理由 + 对比表 + 播种脚本步骤1）、data-directory-convention.md（对比表）、plan.md（播种概要步骤1）

## Phase 9.5 测试数据目录统一方案写入计划
- 在 plan.md 中 Phase 9 和 Phase 10 之间插入 Phase 9.5
- 核心方案：所有测试数据从系统临时目录迁移到 `{repo}/test-data/`（worker/ + e2e/ 子目录）
- 后端 Worker Test：`test-data/worker/data/test-user/`，跑完不清理保留现场
- E2E 测试：`test-data/e2e/`（假视频 + SQLite 直接提交 git，多台电脑零播种）
- 日志统一到 `log/test/worker-tests/` 和 `log/test/e2e/`
- 同步更新 Phase 10 中的 TempDir 引用（播种概要从 10 步简化为 4 步、清理改为 git checkout 重置、产物路径改为 repo 内）

## 测试数据策略文档化
- 在 `doc/testing/backend/test-plan.md` 新增 §4"测试数据策略"章节：详细说明三种数据构造模式（纯函数内联数据、临时文件自建即删、API 端到端空库+自建）
- 包含代码示例和总结对比表（5 个测试类型 × 4 个维度）
- 章节编号全面重排（原 §4-§10 → §5-§11）
- 在 `doc/testing/backend/test-current-suite.md` 每个测试段落（§2-§10）补充一行"数据来源"说明

## plan.md Phase 9.5 补充 e2e-test-data-spec.md 改造需求
- 在 9.5 关联文档更新表格中，扩展 `e2e-test-data-spec.md` 更新范围：整体压缩表述 + 新增独立的"E2E 测试数据配置指南"章节
- 新章节要求细化到可执行步骤：假视频放哪/怎么命名/什么内容、目录怎么建、环境变量怎么设、如何验证配置成功

## Phase 9.5 测试数据目录统一（已完成）
- 改造 `TestBootstrap.cs`：数据目录从 `%TEMP%/jvedio-test-{GUID}/` → `{repo}/test-data/worker/`，日志 → `{repo}/log/test/worker-tests/`，新增 `FindRepoRoot()` + `TestDataDir` 属性，测试后不删除保留现场
- 创建 `test-data/e2e/` 目录结构 + 5 个假视频文件（各 1024 字节），提交到 git
- 重写 `e2e-test-data-spec.md`（215→133 行）：删除与 `data-directory-convention.md` 重复内容，新增"E2E 测试数据配置指南"可执行步骤章节（5 步）
- `.gitignore` 更新：`test-data/e2e/` 跟踪，`test-data/worker/` 忽略（每次测试重建），`log/test/**` 跟踪
- 关联文档同步：`data-directory-convention.md` §6 对比表、`test-plan.md` §6 TestBootstrap、`logging-convention.md` §2.5、`AGENTS.md` 测试数据/日志段落
- 44 个后端测试全部通过，`test-data/worker/data/test-user/` 有 SQLite，`log/test/worker-tests/runtime/` 有日志
- plan.md Phase 9.5 压缩为摘要
- 已提交推送到 origin/master (adf2c29)

## E2E 播种/清理脚本创建
- 创建 `test-data/scripts/seed-e2e-data.ps1`：一键创建目录+假视频+启动 Worker+API 播种+验证+输出 e2e-env.json，支持 `-NoPause` / `-SkipWorkerShutdown` 参数
- 创建 `test-data/scripts/cleanup-e2e-data.ps1`：停 Worker+git checkout 重置数据+清环境变量，支持 `-CleanLogs` / `-NoPause` 参数
- `.gitignore` 更新：跟踪 `test-data/scripts/`，忽略 `e2e-env.json` 等播种产物
- `e2e-test-data-spec.md` §6/§7 重写为实际脚本说明（含步骤表 + 用法示例）
- `AGENTS.md` 测试脚本入口新增 E2E 播种/清理两条
- 已提交推送到 origin/master (b427938)

## E2E 数据架构文档化
- 在 `e2e-test-data-spec.md` 新增 §1.1"数据架构与设计思路"章节（约 70 行）
- 内容涵盖：数据隔离原因（环境变量方案）、完整数据流图（播种→测试→清理）、三类文件职责对比表（e2e-env.json vs SQLite）、与 Worker 后端测试的互补关系对比表
- `doc/testing/README.md` 更新：E2E 阅读顺序调整为 e2e-test-data-spec.md 优先、目录结构补充该文件
- 已提交推送到 origin/master (b75c1db)

## Phase 9.6 数据层流程测试完善计划写入
- 在 `plan.md` 中 Phase 9.5 和 Phase 10 之间插入 Phase 9.6（约 120 行）
- 定位：E2E 自动化测试（Phase 10）的必要前置，补全 MetaTube 抓取 + Actor 数据测试覆盖
- 四个子步骤：9.6.1 播种脚本增加抓取步骤、9.6.2 Actor API 契约测试（3-5个）、9.6.3 MetaTube 抓取集成测试（3-4个，RequiresNetwork）、9.6.4 测试文档更新
- 关键设计：网络依赖优雅降级（抓取失败不终止播种）、Actor 测试推荐预置 SQLite 基线、MetaTube 测试用 TestCategory 条件执行
- Phase 10 前提条件已更新为包含 Phase 9.6
- 同步更新 `handoff.md`（新增 Phase 9.6 回顾条目）和 `validation.md`（Phase 7 加注释说明抓取部分已被 9.6 承接）

## Phase 9.6 增加 9.6.0 测试环境配置基础设施
- 在 plan.md Phase 9.6 中插入 9.6.0 步骤（放在 9.6.1 之前），作为所有后续步骤的前置基础设施
- 核心方案：新建 `test-data/config/test-env.json` 统一配置 MetaTube 地址等可变外部依赖
- 支持 `.local.json` 覆盖机制（个人自部署地址不提交仓库）
- 依赖分析：9.6.0（配置文件）→ 9.6.1（播种脚本读取）→ 9.6.3（抓取测试读取）
- 后端已原生支持 `JVEDIO_METATUBE_SERVER_URL` 环境变量覆盖（LibraryScrapeService 第 47 行）
- 同步更新：9.6.3 方案描述、9.6.5 执行顺序、通过标准（+3 项）、关联文档列表（+2 份）
- 同步更新 handoff.md Phase 9.6 表格（加入 9.6.0 条目 + "已决策"段落）

## 9.6.0 配置文件结构确定为方案 B（平铺）
- 用户确认采用方案 B：`seedVideos`（控制假视频文件创建和库分配）+ `scrapeableVids`（标记 MetaTube 可查 VID）
- 不再维护两套独立 VID 列表（播种脚本硬编码 vs verifiedVids），统一由 test-env.json 驱动
- `seedVideos.libA/libB` 取代播种脚本 Step 2 中硬编码的 `$fakeFiles` 字典
- `scrapeableVids` 取代旧的 `verifiedVids`，明确含义为"这些 VID 在 MetaTube 上可查到"
- 同步更新 plan.md（9.6.0 配置结构 + 消费方式表 + 执行内容 + 9.6.1 全部改写 + 数据就绪表格）和 handoff.md（子项概要 + 已决策列表）
