# 2026-03-19

## plan.md 压缩重写
- 将 `plan/active/desktop-ui-shell-refactor/plan.md` 从 626 行压缩到约 220 行
- 删除了大量 Electron 历史叙事、已完成阶段的冗长描述、重复的非目标/风险段落
- 补充了新架构真实现状：tauri/ 工程结构、依赖清单、页面/组件清单、Phase 6 验证状态
- Phase 0-6 统一用表格简要标注完成状态，Phase 7（E2E 自动化测试）标为待执行
- 已知功能缺口独立成表

## plan.md 阶段路线调整
- 插入 Phase 7（UI 交互完整性补全）：功能缺口表收为正式阶段
- 插入 Phase 8（后端测试工程迁移）：Jvedio.Test 深度绑定旧 WPF，需迁移到 .NET 8 + Worker/Contracts
- 原 E2E 测试顺延为 Phase 9 并标注暂缓（公共电脑环境，数据模拟敏感）
- 同步更新 doc/testing/README.md：后端测试加迁移待办说明，前端 E2E 加暂缓标注

## plan.md 阶段路线再调整
- 插入 Phase 9（日志目录统一）：将散落各处的日志收敛到 `{repo}/log/` 下 runtime/test/dev 分层
- Phase 9 明确标注：启动时先重新收集 Phase 8 后的实际日志输出情况再执行
- 原 E2E 测试顺延为 Phase 10
- Phase 8 第6步改为直接物理删除旧测试工程（非 deprecated）

## Phase 7-10 三轮迭代完善
基于代码核实和关联文档全面审查，对 Phase 7-10 做了深度迭代：
- **Phase 7**：剔除已完成项（"打开文件夹"已接入 opener API、右键菜单已实现），Settings 细化到 3 个子组具体需实现的表单，新增批量操作项和前置依赖检查，补充 5 份关联文档更新清单
- **Phase 8**：从 6 步扩展为 8 步（新增 sln 修改、PS1 脚本重写），迁移表细化到每个测试的类型和注意事项，补充 9 份关联文档更新清单
- **Phase 9**：新增启动时收集清单（7 项），代码改动表细化到具体文件，补充 7 份关联文档更新清单
- **Phase 10**：拆为环境准备 / 数据播种 / 用例执行三个子步骤，加入 48 用例分 Flow 统计表，补充 8 份关联文档更新清单

## Phase 8 后端测试工程迁移（已完成）
- 新建 `Jvedio.Worker.Tests`（.NET 8 / MSTest 3.x / WebApplicationFactory<Program>）
- 13 个契约测试全部通过：Bootstrap(2) + DTO(2) + Libraries(2) + Settings(3) + Videos(4)
- 修复 3 个测试断言错误：camelCase 序列化策略、CreateLibrary 嵌套响应结构、UpdateSettings 必填字段
- 旧 Jvedio.Test 从 sln 移除，评估结论：0 个可直接迁移，5 个高价值需重写（标记技术债）
- 旧 Jvedio.Test 目录用户拒绝物理删除，暂保留
- 关联文档更新完成：AGENTS.md、testing/README.md、test-plan.md、test-targets.md、test-current-suite.md、developer.md、CHANGELOG.md、handoff.md

## Phase 8 收尾：旧测试删除 + 业务测试重写（已完成）
- 物理删除旧测试工程 `Jvedio-WPF/Jvedio.Test/` 目录
- 重写 5 个高价值旧测试到 `BusinessLogicTests/` 目录：
  - VidParsingTests: 17 个数据驱动用例（通过反射调用 ExtractVideoId）
  - SidecarPathTests: 6 个用例（验证 NFO/poster/thumb/fanart 路径，读写一致性）
  - ScanOrganizeTests: 5 个文件系统用例（通过反射调用 TryOrganize）
  - ScanImportApiTests: 2 个 API 端到端用例（扫描导入 + 空目录）
- 测试总数从 13 → 44，全部通过
- 关联文档全部更新
- 已提交推送到 origin/master (d165658)

## 旧 Jvedio.Test 目录彻底清理（已完成）
- 上次物理删除未完全生效（文件仍在磁盘且 41 个文件仍被 git 跟踪）
- 在 `temp/remove-old-test.ps1` 编写清理脚本：Remove-Item + git rm --cached
- 脚本执行后 41 个文件从 git index 移除，目录从磁盘彻底删除
- 已提交推送到 origin/master (2314dc7)

## Phase 8.5 目录更名 `Jvedio-WPF/` → `dotnet/`（已完成）
- `git mv Jvedio-WPF dotnet` 物理更名
- 修改运行时代码 4 个文件：Program.cs（repo root 检测 + 注释）、worker.rs（dev 路径）、shell_log.rs（repo 检测）、prepare-worker.ps1（构建路径）
- 批量替换 29 个文档文件、171 处引用，全局搜索确认零残留
- Worker 编译通过、44 个测试全部通过
- 已提交推送到 origin/master (40e5f61)，521 文件变更

## Phase 9 日志目录统一（已完成）
- `log/` 从平铺结构改为 `runtime/` + `test/` + `dev/` 分层子目录
- Worker `ResolveLogDirectory()` → `log/runtime/`，`JVEDIO_LOG_DIR` 自动追加 `runtime/`
- Shell `resolve_log_dir()` → `log/runtime/`，同样自动追加
- TestBootstrap `JVEDIO_LOG_DIR` 指向 `log/test/worker-tests/`
- 创建 5 个 `.gitkeep` 文件保留目录结构
- `.gitignore` 更新支持子目录例外
- `doc/logging-convention.md` 重写为分层结构
- 关联文档更新：AGENTS.md、testing/README.md、test-plan.md、handoff.md、CHANGELOG.md、plan.md
- Worker 编译通过、44 个测试全部通过
- 已提交推送到 origin/master (6538d72)

## Plan.md 已完成内容简化 + AGENTS 规则
- 简化 plan.md 中 Phase 6-9 的已完成部分：从详细步骤/表格/通过标准压缩为 2-5 行摘要
- plan.md 从 ~645 行压缩到 ~330 行
- 在 AGENTS.md 增强规划工作流中新增规则：完成步骤后必须回 plan.md 简化已完成内容
