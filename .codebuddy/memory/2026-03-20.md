# 2026-03-20

## 人工验收计划创建
- 在 `plan/active/manual-acceptance-v010/` 创建了 v0.1.0 人工验收计划
- 包含 plan.md（10 个 Phase、50+ 验收项）、handoff.md、plan.json
- 提交 `851b8c7` 并推送到远程
- 下一步：用户解压 ZIP 便携版后按 Phase 1-10 顺序执行人工验收

## Release 发布格式改造：NSIS → ZIP 便携版
- 移除 NSIS 安装包发布模式，改为仅发布 ZIP 便携版（解压即用）
- 变更文件：tauri.conf.json（targets: []）、删除 copy-release.ps1、新增 package-portable.ps1、package.json 脚本更新
- 同步更新 AGENTS.md、验收计划 plan.md 和 handoff.md
- Plan 文档：`plan/active/tauri-release-packaging/`
- 提交 `ef47856` 并推送
- 下一步：执行 `npm run build:release` 验证 ZIP 打包流程

## ZIP 打包流程验证通过
- `npm run build:release` 完整跑通
- 发现：`bundle.targets=[]` 时 Tauri 产出 `jvedio-shell.exe`（Cargo crate 名），不是 `JvedioNext.exe`
- 修正 `package-portable.ps1`：自动查找并重命名为 `JvedioNext.exe`
- 产出 `JvedioNext_0.1.0_x64-portable.zip`（5.95 MB），包含 exe + dll + resources/ + worker/(29 文件)
- 待人工验收：解压后双击 JvedioNext.exe 是否正常启动
