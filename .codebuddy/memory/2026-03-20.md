# 2026-03-20

## Phase 9.6 数据层流程测试完善（已完成）
- 继续前次未完成的工作：9.6.2+9.6.3 的测试文件已创建但未提交
- 修复 `ScrapeApiTests.ScrapeLibrary_ValidLibrary_ReturnsAccepted` 测试失败：`data.dbId` → `data.library.libraryId`（与 LibrariesApiTests 一致）
- 52 个测试全部通过（原 44 + Actor 5 + Scrape 3）
- 提交推送 9.6.2+9.6.3 测试代码（1c5676f）
- 完成 9.6.4 测试文档更新：test-targets.md、test-current-suite.md、test-plan.md、e2e-test-data-spec.md、README.md、AGENTS.md、CHANGELOG.md、plan.md、handoff.md
- 提交推送 9.6.4 文档（4b4abe7）
- Phase 9.6 全部 5 个子步骤（9.6.0-9.6.4）已完成，MetaTube P0 技术债已消除

## 测试配置指南文档完善
- 重写 `test-data/config/README.md`：新增快速上手章节（两种场景对比、E2E 播种四步流程、自定义常见场景表、关键文件一览树形图）
- 更新 `doc/testing/README.md`：新增"快速上手"章节，含速查表引导到配置文档

## 文档一致性修复（11 处不一致）
- `test-targets.md`：修复 §3 重复编号（→§4-§8）、旧工程路径 `Jvedio.Test`→`Jvedio.Worker.Tests`、旧数字 18→52
- `testing/README.md`：旧工程名注释 `Jvedio.Test C#`→`Jvedio.Worker.Tests C#`
- `test-plan.md`：移除过时的契约测试硬编码数量 13
- `AGENTS.md`：修正 VidParsing 17→18、移除不存在的 UrlParamBypassed 1、旧工程名引用修复
- `playwright-e2e-test-cases.md`：旧数据目录 `dotnet/Jvedio.Test/config/scan/input/`→`test-data/e2e/videos/`、MetaTube 配置路径更新
- `ui-todo.md`：旧工程名 `Jvedio.Test`→`Jvedio.Worker.Tests`
- `plan.md`：Phase 9.5 状态标记修正为 ✅、Phase 9.6 详细内容压缩为 2-3 行摘要（删除约 260 行详细步骤），清理重复的 Phase 10 标题

## 演员头像缓存路径文档补全（3 个文件）
- `e2e-test-data-spec.md`：补充数据流图中 MetaTube 抓取产物分支、§2 目录树增加 cache/actor-avatar/、新增 §5.1 抓取后目录变化（sidecar + 演员头像产物表）、§6 播种步骤增加 Step 5.5-5.9、三类文件职责表增加 sidecar 和演员头像
- `test-data/config/README.md`：关键文件树增加 sidecar 和 actor-avatar 目录、播种步骤第 5 步增加产物输出位置说明
- `test-targets.md`：§3.3 增加正式 vs E2E 测试路径对照表、§3.4 修正旧工程名 Jvedio.Test→Jvedio.Worker.Tests 并更新为当前架构

## E2E Sidecar 路径迁移计划
- 用户决定：只改测试环境下的 sidecar 路径，Release 工程代码不动
- 采用方案 B：`data/{UserName}/cache/video/{LibName}/{VID}/` — 按库名分子目录
- 本次实施策略：先只改文档，记录 E2E 目标目录结构；播种脚本和 Worker 代码的实际路径适配留到后续
- 创建了 `plan/active/e2e-sidecar-path/plan.md` 和 `handoff.md`
- 排查确认 5 个需要更新的文档：e2e-test-data-spec.md、test-targets.md、test-data/config/README.md、AGENTS.md、data-directory-convention.md

## E2E Sidecar 路径迁移执行（Phase 1-3 完成）
- Phase 1 跳过（播种脚本不改，留后续 Phase 4）
- Phase 2 完成：5 个文档全部更新，E2E sidecar 路径统一为 `data/{UserName}/cache/video/{LibName}/{VID}/`（提交 d8912da）
- Phase 3 完成：文档一致性验证通过、.gitignore 覆盖确认、cleanup 脚本覆盖确认
- plan.md 已完成内容已压缩，通过标准全部勾选（提交 a4e7c0b）
- Phase 4（Worker 测试环境路径适配）已完成

## E2E Sidecar 路径迁移执行（Phase 4 完成）
- `WorkerPathResolver.cs`：新增 `VideoCacheFolder` + `IsTestEnvironment`（通过 `JVEDIO_APP_BASE_DIR` 环境变量判断）
- `LibraryScrapeService.cs`：新增 `ResolveSidecarDirectory()`，`WriteSidecarsAsync` / `NeedsScrape` / `LoadCandidates` 传入 `libraryName` 走统一路径路由
- `VideoService.cs`：注入 `WorkerPathResolver`，`BuildSidecarState` / `DeleteVideo` / `LoadFavoriteVideos` / `LoadVideosByGroup` 支持 E2E 路径路由
- `SidecarPathTests.cs`：+3 测试（`VideoCacheFolder_Property_Exists`、`ResolveSidecarDirectory_Method_Exists`、`ResolveSidecarDirectory_Production_FallsBackToVideoDirectory`）
- `seed-e2e-data.ps1`：Step 5.9 验证路径改为 `cache/video/E2E-Lib-A/{VID}/`，保留旧路径 fallback
- `cleanup-e2e-data.ps1`：新增 `cache/` 目录显式删除
- 55 个测试全部通过，提交推送 `6db3941`
- plan.md + handoff.md 已更新标记完成

## E2E Sidecar 路径迁移归档
- 代码一致性验证：6 个源码/脚本文件全部通过
- 文档一致性验证：7 个文档 E2E sidecar 路径描述统一为 `data/{UserName}/cache/video/{LibName}/{VID}/`
- 归档：`plan/active/e2e-sidecar-path/` → `plan/archive/e2e-sidecar-path/`
- 提交推送 `0d5a2e2`，Feature 全部完成

## 后端 API 校验脚本
- 新建 `test-data/scripts/verify-backend-apis.ps1`，覆盖 Worker 全部 31 个 API 端点（8 个 Controller）
- 基于 seed-e2e-data.ps1 播种后的 e2e-env.json 读取连接信息
- 支持 `-BaseUrl` 手动指定、`-NoPause` CI 模式
- 跳过破坏性操作（delete video/batch delete）保护播种数据
- AGENTS.md 新增脚本入口条目
- 提交推送 `54595d4`

## 后端 API 校验脚本关联文档同步
- 更新 7 份文档，同步 `verify-backend-apis.ps1` 的引用和说明：
  1. `test-data/config/README.md` — 目录树 + 消费者表
  2. `doc/testing/README.md` — 快速操作表
  3. `doc/CHANGELOG.md` — [未发布] 变更记录
  4. `doc/testing/e2e/e2e-test-data-spec.md` — 新增 §6.5 + 维护规则
  5. `doc/testing/backend/test-targets.md` — 验证目标说明
  6. `plan.md` Phase 10 — 修正播种脚本路径 + 补充 verify 步骤
  7. `handoff.md` Phase 9.6 — 新增 9.6.5 子项
- 提交推送 `c3dae16`

## plan.md 已完成内容压缩
- Phase 0-9.6 各 Phase 详细执行步骤压缩为 1 行摘要表格（共 10 行表格替代 ~50 行分散段落）
- 验证矩阵（架构/页面/事件三级 16 行）压缩为 1 行结论
- 净减少 56 行（383→327）
- 提交推送 `6fb665b`

## 新建 Feature: scrape-fail-graceful（抓取失败优雅降级）
- 创建 `plan/active/scrape-fail-graceful/plan.md` + `handoff.md`
- 模仿 Jellyfin 处理：抓取失败仍创建 sidecar 目录 + stub NFO，前端显示缺失海报占位图
- 8 个 Phase：DB 迁移 → Contracts → Worker 逻辑 → API 筛选 → 前端 → 测试(+7) → E2E 脚本 → 文档(8份)
- 影响 ~17 个文件，新增 ScrapeStatus 字段（none/full/failed）

## scrape-fail-graceful plan 第二轮迭代
- 新增 Phase 4.5：右键「重新获取元数据」前后端完整链路（8 个子步骤）
- 关键发现：后端 `StartLibraryScrapeRequest.VideoIds` 已支持单影片搜刮，前端类型定义落后（只有 `forceRescrape`），需对齐
- 新增前端改动：LibraryPage/FavoritesPage 右键菜单 + VideoDetailPage 搜刮按钮 + BootstrapContext SSE 回调增强
- DTO 补充：`VideoListItemDto` + `VideoDetailDto` 需新增 `libraryId` 字段（FavoritesPage 右键搜刮需要）
- 测试扩展：+7→+10（52→62），新增 3 个重新搜刮链路测试
- i18n 扩展：common.json 新增 3 条 rescrape 文案
- 通过标准新增 8 项重新搜刮验证点
- 风险新增 4 项（任务冲突 409、单影片走全库端点、DTO 对齐兼容性、FavoritesPage libraryId）
- plan.md 从 377→608 行，handoff.md 同步更新

## scrape-fail-graceful plan 第三轮迭代（用语修正）
- 用户指出：UI 设计规格 `doc/UI/new/dialogs/video-context-menu.md` 已将"重新抓取元数据"定义为右键菜单 6 标准项之一
- 代码现状：`LibraryPage.tsx` / `FavoritesPage.tsx` 的 `getContextMenuActions()` 只实现了 5 项（缺 rescrape），i18n 中也没有相关 key
- 修正 Phase 4.5 标题和描述：从"新增菜单项"改为"按设计规格补齐缺失的菜单项 + 接入真实搜刮 API"
- 修正影响面汇总表、通过标准、handoff.md 中的对应描述
- handoff.md 关键发现新增第 6 条：UI 设计规格已定义但代码未实现

## scrape-fail-graceful plan 第四轮迭代（UI 规格对齐审查）
- 逐项对比 plan.md 中所有 UI 改动点 vs `doc/UI/new/` 下 24 个定稿文档
- 确认原则：**不新增设计规格中没有定义的 UI 元素，只改后端响应逻辑**
- 删除 5 项超出设计规格的 UI 新增：
  1. VideoDetailPage "重新获取元数据"按钮（Phase 4.5.6）— 设计规格中详情页无此按钮
  2. VideoDetailPage 失败横幅 `scrape-failed-banner`（Phase 5.5a）— 设计规格中无此元素
  3. VideoCard `scrape-failed-badge` 文字标签（Phase 5.3）— 违反"不扩展成大 badge 区"规则
  4. `showToast` 弹窗反馈（Phase 4.5.4/4.5.8）— 设计规格中无 Toast 组件，任务反馈走行内摘要/活动条
  5. i18n 文案删减：`scrapeFailed.banner`、`rescrapeStarted`、`rescrapeCompleted` 三条删除
- 保留的合规 UI 改动：
  - 右键菜单补齐"重新抓取元数据"菜单项（设计规格已定义）
  - VideoCard emoji 替换为 SVG 占位图（设计规格提到"占位"）
  - sidecar StatusBadge 适配 stub 场景（设计规格已定义 synced/failed 变体）
  - SSE 搜刮成功后行内摘要自动回刷（纯数据层）
- plan.md + handoff.md 同步更新

## scrape-fail-graceful plan 第四轮迭代补充（文档更新表述修正）
- 重新检查 Phase 8（文档更新）的 8 个条目，发现 4 个问题：
  1. `plan/active/desktop-ui-shell-refactor/plan.md` — "Phase 10 前提条件引用"指向不存在的内容（desktop-ui-shell-refactor 最新阶段为 Phase 9.6，没有 Phase 10），已删除该条目
  2. `doc/CHANGELOG.md` — 变更条目描述需排除已删除的 UI 元素，已补充明确范围说明
  3. `doc/testing/README.md` — "如有新操作入口则更新"措辞不准确（右键菜单是补齐不是新增），已改为"更新后端测试数量和最新覆盖范围"
  4. 所有 8 个条目都缺少具体操作说明，已为每个条目新增"说明"列
- 文档数量从 8 个调整为 7 个（删除了不存在的 Phase 10 引用）
- 修正 Phase 5.2 中 `VideoDetailDto.libraryId` 注释引用：从已删除的 Phase 4.5.6 改为 Phase 4.5.5
- 修正风险表中 409 冲突场景的反馈方式：从"toast 提示"改为"菜单项层面禁用或 pending 状态"
- 通过标准中"文档全部同步"改为列出具体 7 个文档名
- handoff.md Phase 8 行补充具体文档数量和列表

## scrape-fail-graceful Feature 执行完成
- 按 handoff.md 建议的执行顺序 `2→3→1→4→4.5→5→6→7→8` 全部完成
- 8 个提交（含 PowerShell git 引号问题修复后的格式）：
  - `e058d27` Phase 2 — DB 迁移（ScrapeStatus 列）
  - `44d3292` Phase 3 — Contracts DTO（ScrapeStatus 字段）
  - `445f5cb` Phase 1+4 — Worker stub sidecar + ScrapeStatus 筛选
  - `2029778` Phase 4.5+5 — 前端右键重新搜刮 + 占位图 + SSE + i18n
  - `559b719` Phase 6 — 10 个新测试（52→62）
  - `f2349c3` Phase 7 — E2E 脚本 scrapeStatus 验证 + videoIds 搜刮端点
  - `5aaf168` Phase 8 — 7 个文档更新
  - `8b28bba` 文档收尾 — handoff 进度表全部 ✅ + plan.md 已完成 Phase 压缩
- 62 个测试全部通过
- 下一步：可归档 `plan/active/scrape-fail-graceful/` → `plan/archive/scrape-fail-graceful/`

## desktop-ui-shell-refactor 归档
- Phase 10 前端 E2E 验收已确认全部完成（7 组 flow 通过 + 抓取失败降级通过 + 产物齐备）
- 归档：`plan/active/desktop-ui-shell-refactor/` → `plan/archive/desktop-ui-shell-refactor/`
- 更新 15 处文档中的路径引用（plan.md / handoff.md / ui-todo.md / doc/UI/new/README.md / foundation/README.md / testing/README.md / playwright-e2e-test-plan.md / playwright-e2e-test-cases.md）
- `doc/CHANGELOG.md` 中的历史记录保持原样（纯叙述性）
- `plan/active/` 目录现为空，当前无 active feature

## 新建 Feature: tauri-release-packaging（Tauri 完整打包）
- 创建 `plan/active/tauri-release-packaging/` 活动计划目录
- 包含 4 个文档：plan.md、handoff.md、open-questions.md、validation.md + plan.json
- 环境确认：Rust 1.94.0 + .NET 9.0.312 + Node v24.14.0 + Tauri CLI 2.10.1 全部就绪

## tauri-release-packaging 架构决策：去掉 WPF 启动层
- 用户确认 3 个决策：(1) 不保留 WPF 回退 (2) 需要单实例控制 (3) exe 名称 `JvedioNext`
- 新增 Phase 0（去掉 WPF 启动层 + 单实例控制），插在原 Phase 1 前
- 新架构 2 层：`JvedioNext.exe` (Tauri) → `Jvedio.Worker.exe` (.NET 8)
- 核心代码改动 6 项（C-1~C-6）：tauri.conf.json / package.json / Cargo.toml / lib.rs / App.xaml.cs / Jvedio.csproj
- 关联代码检查 4 项（C-7~C-10）：worker.rs / shell_log.rs / main.rs / WorkerPathResolver
- 文档同步 9 项（D-1~D-9）：AGENTS.md / developer.md / 13-tauri-shell-worker.md / data-directory-convention.md / logging-convention.md / CHANGELOG.md / handoff.md / open-questions.md / validation.md
- 新增 OQ-7（Worker SharedAppBaseDirectory 安装包推断）+ OQ-8（single-instance 版本兼容）
- 全量 7 个 Phase、40 个验证项（原 23 → 40）

## tauri-release-packaging 全部 7 个 Phase 执行完成
- Phase 0：去掉 WPF 启动层 + 单实例控制（6 个代码改动 + 4 个检查 + 6 个文档更新）→ 提交 `802647a`
- Phase 1-3：Worker / 前端 / Rust 壳层编译验证全部通过 → 提交 `fa1705e`
- Phase 4：NSIS 安装包 `JvedioNext_5.0.0_x64-setup.exe`（9.8 MB）成功产出；MSI 因 WiX 问题暂跳过 → 提交 `fa1705e`
- Phase 5：安装验证通过（双进程运行 + 单实例控制生效）；修复 SQLite native DLL 丢失（改用 `dotnet publish`）→ 提交 `1b3022c`
- Phase 6：文档收口 + .gitignore 补全 + plan.md 已完成 Phase 压缩 → 提交 `a98e65e`
- 关键修复：`prepare-worker.ps1` 从 `dotnet build` 改为 `dotnet publish -r win-x64 --self-contained false`
- `bundle.targets` 设为 `["nsis"]`（绕过 WiX light.exe 失败）
- Feature 全部完成，可归档

## tauri-release-packaging Phase 7+8 plan 编写
- Phase 7 — 统一构建输出目录：plan.md 新增完整方案（目标 `build/` 收归产物）
  - Step 0 — 清理现有散落构建产物：盘点 `tauri/worker-dist/`(26 files, 2.83MB) + `tauri/dist/`(4 files, 0.35MB) + bundle 旧残留(msi + 旧名 nsis)
  - 7.1~7.8 核心改动 + 7.9~7.13 关联检查（均不受影响）+ 7.14~7.21 文档更新
- Phase 8 — Release Tag 版本管理：版本号从 `5.0.0` 重设为 `0.1.0`
  - 4 个核心文件需改版本号：package.json / tauri.conf.json / Cargo.toml / Jvedio.csproj
  - Git tag `v0.1.0` + CHANGELOG 切割 + 版本管理约定写入 AGENTS.md
  - OQ-3 将从"保持 5.0.0"更新为"重设 0.1.0"

## tauri-release-packaging Phase 7+8 执行完成
- Phase 7（统一构建输出目录）：
  - Step 0 清理旧散落产物（worker-dist 26 files + dist 4 files + 旧 NSIS/MSI 残留）
  - 7.1~7.2 更新 .gitignore（根 `/build/`，tauri 移除旧规则）
  - 7.3 重写 `prepare-worker.ps1` 直接 publish 到 `build/worker-stage/`
  - 7.4~7.6 修改 tauri.conf.json（bundle.resources + frontendDist）和 vite.config.ts（outDir）
  - 7.7~7.8 新建 `copy-release.ps1` + 更新 `build:release` 脚本
  - 7.14~7.21 同步更新 7 份文档
  - `npm run build:release` 验证通过
  - 提交 `a2b39a6`~`39f69b4`（4 个提交）
- Phase 8（Release Tag 版本管理）：
  - 版本号从 `5.0.0` → `0.1.0`（4 个核心文件）
  - CHANGELOG 切割 `[未发布]` → `[0.1.0]` 区块
  - AGENTS.md 新增版本管理约定
  - `npm run build:release` 验证产出 `JvedioNext_0.1.0_x64-setup.exe`（2.99 MB）
  - Git tag `v0.1.0` 创建并推送到远程
  - 提交 `554780b`~`d093194`（2 个提交）
- 全部 8 个 Phase（0-8）已完成，tauri-release-packaging Feature 可归档
