# Electron C-1 Scaffold

This directory contains the Stage C-1 shell scaffold for the new Electron desktop entry.

- `main/`
  - Electron main-process lifecycle and worker bootstrap.
- `preload/`
  - Minimal bridge for app version and worker base URL.
- `renderer/`
  - Placeholder shell page used to validate the startup chain.
