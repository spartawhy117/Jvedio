# Shared UI Components Spec

## 页面目的

- 统一当前 exe 各页面重复出现的基础 UI 组件。
- 页面规格文档不再各自重复解释相同组件，只引用本文件。

## 适用范围

- `库管理`
- `库内容`
- `喜欢`
- `演员`
- `演员详情`
- `影片详情`
- `设置`
- 各类弹层

## 通用文档规则

- 线框图负责：
  - 空间布局
  - 信息层级
  - 主要视觉关系
- 页面 `.md` 负责：
  - 元素功能
  - 数据来源
  - 状态定义
  - 交互规则
  - 性能与回归点

## 统一影片卡片

### 目的

- 为 `库内容`、`喜欢`、`演员详情关联影片` 提供统一的影片结果项结构。

### 组成

- 大图主视觉区
- 右上角勾选按钮
- 图底细进度 / 状态条
- 居中主标题
- 居中次级元数据行
- 悬浮或右键动作入口

### 规则

- 结构参考 Jellyfin 的 `backdrop` 影片卡，优先使用宽图主视觉，而不是左图右文的资料块。
- 图像区占卡片绝大多数高度，主视觉优先让给影片海报或主图展示。
- 卡片下半文字区尽量压缩，只保留标题和年月两行，不再扩展说明块。
- 卡片右上角保留勾选按钮，未勾选时只表示可选；用户勾选后表示进入批量操作选择态。
- 主标题优先用 `VID + 标题短名` 的合并写法，避免再拆多行身份信息。
- 次级元数据只保留一行年月信息，例如 `2026.03`，不再同时堆厂牌和状态。
- 播放进度、已整理或已刮削等状态优先通过主图底边细条表达，不扩展成大 badge 区。
- 点击卡片或主链接进入 `video-detail-page`。
- 右键或 `更多` 按钮统一打开 `dialogs/video-context-menu.md` 定义的动作菜单。
- 当已勾选至少 1 张影片卡后，右键当前选中卡片或选中区域，统一打开 `dialogs/video-batch-context-menu.md` 定义的批量菜单。

## 统一演员卡片

### 目的

- 为 `演员` 聚合页以及后续任何演员结果网格提供统一的人像卡片结构。

### 组成

- 竖向头像主视觉区
- 居中演员名
- 居中次级信息行

### 规则

- 结构参考 Jellyfin 的 `person card`，优先保留大头像和居中文本，不退回资料面板式卡片。
- 头像区保持固定竖向比例，窗口伸缩时不改成横图或自由裁切块。
- 次级信息行默认只保留一行，优先展示 `作品数`，必要时才替换为别名或角色说明。
- 演员卡默认不承载复杂 badge、长简介或多行操作区。
- 点击卡片进入 `actor-detail-page`。

## 结果摘要条

### 目的

- 在聚合结果页顶部用短标签快速表达当前结果集状态。

### 组成

- 总数
- 缺资源数量
- 当前筛选状态

### 规则

- 只表达当前结果集状态，不重复页面标题。
- 采用短词和数字，不写长句说明。
- 当结果页支持返回链路恢复时，摘要条可承载当前筛选与当前选择对象的短说明。

## 结果页查询工具栏

### 目的

- 为 `库内容`、`喜欢`、`演员` 提供统一的查询、排序与刷新入口。

### 组成

- 关键字搜索框
- 刷新按钮
- 排序下拉按钮
- 排序状态短标签

### 规则

- 工具栏保持单行优先，桌面宽度足够时不拆成多排杂糅布局。
- 搜索框始终放在左侧，承担当前页面最核心的关键字筛选输入。
- 搜索框与刷新按钮紧邻排布，刷新按钮语义固定为重新拉取当前结果集，而不是重置所有查询条件。
- 工具栏右侧统一使用一个排序下拉按钮，不再拆成多个横向排序切换按钮。
- 排序下拉面板允许单次展开后直接切换排序规则，用户点击任一选项后立即执行排序并收起面板。
- 影片结果页默认支持以下排序项：
  - `按VID顺序`
  - `按VID倒序`
  - `按名称顺序`
  - `按名称倒序`
  - `按发布日期顺序`
  - `按发布日期倒序`
  - `按导入时间顺序`
  - `按导入时间倒序`
- 其它结果页可复用同一骨架，但下拉内的排序项应按当前实体裁剪，不强行共用影片字段。
- 当前已生效的排序状态可用短标签贴在排序按钮下方或右侧，不重复展开成长说明句。

## 行内 Action Strip

### 目的

- 把单条记录上的多个操作收口到同一行，避免顶部按钮区和行内按钮区重复。

### 适用对象

- 媒体库行
- 任务行
- 其它单条记录管理项

### 规则

- 所有按钮保持在同一水平线上。
- 默认线框示例固定为：
  - `打开`
  - `扫描`
  - `编辑`
  - `删除`
- 动作顺序遵循：
  - 主浏览动作
  - 主执行动作
  - 编辑动作
  - 删除动作
- 样式约定：
  - 主浏览动作：次按钮或链接按钮
  - 主执行动作：主按钮
  - 编辑动作：次按钮
  - 删除动作：危险按钮

## 状态 Badge

### 目的

- 用短词而不是长句表达当前状态。

### 规则

- 一个元素只表达一个状态。
- 状态词优先固定枚举，不在页面里自由发挥。
- 颜色语义固定：
  - 绿色：待执行 / 可处理
  - 蓝色：进行中
  - 灰色：稳定 / 已同步
  - 红色：失败 / 风险

## 结果状态组件

### 目的

- 为聚合结果页和弹层内结果区统一 `Loading / Empty / Error` 的反馈表达，避免每页各自拼装。

### 组成

- loading 骨架或占位块
- empty 文案区
- error 文案区

### 规则

- `Loading` 优先使用骨架块或占位条，不直接堆叠长句提示。
- `Empty` 只表达当前结果为空及最小引导，例如 `暂无结果` 与 `清空筛选后再试`。
- `Error` 只提供短错误说明，不在状态区内额外放置 `重试` 按钮。
- 结果页的重新请求统一通过查询栏 `刷新` 承接，不在状态组件里重复放第二个恢复入口。
- 结果状态组件默认居中放在结果区主内容位置，不抢占页面标题与工具栏区域。
- 同一结果区一次只显示一种主状态，不叠加多个提示层。

## 分页控件

### 目的

- 为结果集页提供统一翻页结构。

### 规则

- 默认由：
  - 上一页
  - 当前页文本框
  - 下一页
  - 跳转执行按钮
  组成。
- 放置在结果区右下角。
- 分页控件采用单行布局，顺序固定为：
  - `<`
  - `当前页文本框`
  - `>`
  - `跳转`
- 中间文本框默认显示 `当前页 / 总页数`，例如 `1 / 24`。
- 用户点击中间文本框后，可直接输入目标页码。
- `跳转` 动作读取中间文本框的输入页码，先校验是否合法，再直接切换到目标页并刷新当前结果集。
- 跳转完成后，中间文本框恢复为新的 `当前页 / 总页数` 显示，例如 `12 / 24`。
- 当输入页码小于 `1` 或大于总页数时，应阻止提交并保留当前页状态。
- 分页状态应尽量写入当前路由或返回态，保证从详情页返回后恢复原上下文。

## 返回链路状态

### 目的

- 为 `库内容`、`喜欢`、`演员`、`演员详情`、`影片详情` 提供统一的“来源页恢复”语义。

### 规则

- 结果页负责持有自己的筛选、排序和分页状态。
- 详情页优先通过 `backTo` 或等效返回态恢复来源页，而不是硬编码返回某个固定页面。
- 若存在二级详情关系，例如 `Actors -> Actor Detail -> Video Detail`，返回链路应先回二级详情，再回结果页。

## 通用确认对话框

### 目的

- 为删除确认、退出确认、覆盖确认这类短路径确认场景提供统一弹窗骨架。
- 调用方只传入中间确认文本，不为每个场景重新定义底部动作结构。

### 组成

- 标题
- 关闭按钮
- 外部传入的正文文本区
- `取消` 按钮
- `确认` 按钮

### 规则

- 使用标准白底模态对话框，不退回轻量菜单或页内提示条。
- 标题、正文、危险说明由调用方按场景传入，但底部按钮固定为 `取消` 与 `确认`。
- 正文文本区承载当前确认语义，可展示单段说明或 1 到 2 行摘要，不扩展为复杂表单。
- 关闭动作始终存在：
  - 右上角关闭
  - 底部 `取消`
- 底部按钮顺序固定：
  - 左侧 `取消`
  - 右侧 `确认`
- 普通确认使用主按钮样式；危险确认可将右侧 `确认` 切换为危险视觉，但不改变按钮顺序与文案。
- 需要完整字段录入的 `create/edit` 场景，不使用本组件，继续使用独立表单弹层。

## 列表页信息密度规则

- 列表页优先展示：
  - 身份信息
  - 数量信息
  - 最近状态
  - 行动入口
- 列表页避免展示：
  - 长路径全文
  - 多行配置细节
  - 只有进入编辑态才需要的信息

## 状态说明文本

- 列表中的次级说明文本只保留一行短句。
- 用来补充 badge 无法表达的上下文。
- 不用于堆配置项。

## 文档模板建议

- 每个页面规格至少包含：
  - `页面目的`
  - `页面范围`
  - `数据来源`
  - `元素清单`
  - `交互规则`
  - `状态定义`
  - `性能与体验约束`
  - `回归点`

## 当前特别约束

- 没有真实实现的功能，不要只因为旧设计稿里出现过就继续画成可点击入口。
- 如果某功能当前不存在稳定产品链路：
  - 可以在文档里说明原因
  - 但不应继续占据页面主操作位
