# Legacy Planning Docs

This directory records legacy planning-oriented references that existed before the enhanced `plan-todo` workflow was initialized.

Current preserved references:

- `../../plan/archive/metatube-only-plan/README.md` - archived MetaTube historical feature plan
- `../test-plan.md` - test-process planning document retained as project documentation

New feature planning should use the normalized `plan/` structure.
