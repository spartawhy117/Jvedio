# Archived Legacy Plan: MetaTube Only

This archive entry preserves the historical MetaTube feature plan that previously lived at `doc/metatube-only-plan.md`.

## Source

- Original document path: `doc/metatube-only-plan.md`
- Archived content snapshot: `plan/archive/metatube-only-plan/original.md`
- Source marker: `plan/archive/metatube-only-plan/legacy-source.path.txt`

## Notes

- The full legacy plan content is preserved in `original.md` so the original `doc/` copy can be safely removed.
- This archive entry marks the feature as historical and prevents future work from treating it as the active planning format.
- New or resumed feature planning should use `plan/active/<feature>/` with `plan.json`, `plan.md`, `.plan-original.md`, and `handoff.md`.

## Status

- Archived from legacy planning format
- Historical feature only
- Not an active feature plan
