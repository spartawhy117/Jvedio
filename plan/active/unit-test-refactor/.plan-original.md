## Baseline Plan Draft

### Feature
- slug: `unit-test-refactor`
- title: `unit test refactor`

### Requirement Summary
- Normalize unit-test structure inside `Jvedio.Test`.
- Keep the first pass limited to the test project.
- Preserve existing runtime path rules and output separation.

### Technical Approach
- Review current `UnitTests`, `IntegrationTests`, and legacy `ScanTest`.
- Normalize directory and naming conventions.
- Clean up floating or empty test files.
- Define a shared testing helper pattern for bootstrap and temp-path isolation.
- Sync test documentation after structural cleanup.

### Current Findings
- `ScanTest/ImportTest.cs` is legacy and does not fully match the current layered structure.
- `UnitTests/Core/Crawler/CrawlerServer.cs` is an empty shell file and is not compiled by `Jvedio.Test.csproj`.
- Several tests mutate static path state directly without a shared restore strategy.
- Runtime path and output rules are already persisted in docs and code.

### Option-Path Comparison
- Path A: minimal test-only refactor inside `Jvedio.Test` only. Recommended.
- Path B: include production seam cleanup. Not recommended for this feature.

### Validation Strategy
- Ensure directory structure and csproj compile items are consistent.
- Ensure docs reflect the final structure.
- Ensure affected tests remain runnable.

### Approval Snapshot
- Planning status: `approved`
- Execution approved: `true`
